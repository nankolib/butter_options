{
  "name": "opta-taker",
  "private": true,
  "version": "0.1.0",
  "description": "Treasury taker — buyer of last resort for user asks. DRY_RUN by default; see src/eligibility.ts for the anti-exploit framework.",
  "type": "commonjs",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "tsc && node scripts/copy-idl.js",
    "start": "node dist/taker/src/main.js",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && node scripts/run-tests.js"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.4",
    "better-sqlite3": "^11.3.0",
    "bn.js": "^5.2.3",
    "bs58": "^5.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/bn.js": "^5.2.0",
    "@types/node": "^24.12.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  }
}
